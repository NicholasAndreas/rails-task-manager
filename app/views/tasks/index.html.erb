<h1> All our Tasks</h1>

<%  @tasks.each do |task| %>
  <h2><%= task.name %></h2>
  <p><%= task.description %></p>
  <%= link_to "More details", task_path(task) %>
  <%= link_to "Delete", task_path(task), method: :delete %>

<% end %>
